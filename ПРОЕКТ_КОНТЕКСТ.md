# ПРОЕКТ_КОНТЕКСТ.md

## 🎯 СУТЬ ПРОЕКТА
- Одностраничная мини‑игра Crash в стиле макета [`OneUP`](https://tashkentskiygg.github.io/OneUp/), оформленная в оранжевой палитре под бренд с зайцем‑ракетой.
- Игрок делает ставку в виртуальных кристаллах, множитель растёт до случайного момента краша; задача — успеть забрать выигрыш до падения.
- Текущая реализация — только фронтенд (React + TypeScript + Vite) с имитацией мультиплеера на стороне клиента.

## ✅ ЧТО СДЕЛАНО (25.02.2026)
### Базовый каркас и сборка
- Инициализирован React+TypeScript проект на Vite (`package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`).
- Настроена общая верстка мобильного вертикального «экрана телефона» (`App.tsx`, `global.css`, `app-layout.css`).
- Проверена успешная сборка (`npm run build`).

### Интерфейс Crash‑экрана
- Реализована верхняя панель `TopBar` с отображением онлайна и баланса кристаллов.
- Сверстан основной блок `CrashScreen` с тёмным космическим фоном, крупным множителем по центру и рядком последних коэффициентов.
- Добавлена нижняя панель ставок `BetPanel` и список игроков `PlayersList`, имитирующий многопользовательскую игру.
- Реализована нижняя навигация `BottomNav` c вкладками «Топ / Краш / Профиль».

### Заяц‑ракета и анимации
- Создан компонент `RocketBunny`, размещённый по центру игрового поля.
- Реализовано плавное «взлётное» движение за счёт CSS‑переменной `--rocket-height`, завязанной на текущий множитель.
- Добавлено медленное покачивание ракеты в полёте (анимация `rocketFloat`) и короткий эффект тряски при краше (`rocketShake`).

### Логика краша и ставок
- Реализован хук `useCrashGame` с фазами `waiting / running / crashed`, ростом множителя и случайным `maxMultiplier`.
- Настроен рост множителя с таймером, остановка на краше и автоматический переход в состояние ожидания.
- Добавлена логика ставки игрока: фиксируем сумму, считаем выигрыш при своевременном `cashout`, обнуляем ставку после завершения раунда.
- `PlayersList` генерирует псевдослучайных игроков и пересоздаёт список при смене фазы раунда.

### Тестирование
- Настроен Vitest + Testing Library (`tests/setup.ts`, настройки в `vite.config.ts`, зависимости в `package.json`).
- Написаны юнит‑тесты `useCrashGame.test.tsx` (старт состояния, рост и краш, кэш‑аут и расчёт выигрыша).
- Написан базовый DOM‑тест `CrashScreen.test.tsx`, проверяющий начальное состояние экрана и наличие кнопки «Сделать ставку».
- Все тесты проходят (`npm test -- --runTestsByPath tests/useCrashGame.test.tsx tests/CrashScreen.test.tsx`).

## 🚧 НАД ЧЕМ РАБОТАТЬ ДАЛЬШЕ
- [ ] Подключить реальный арт зайца‑ракеты через `public/` или `src/assets` (сейчас используется путь `/bunny-rocket.jpg`, ожидающий файл в публичной директории).
- [ ] Точнее подогнать отступы, размеры шрифтов и цвета под пиксели макета (особенно размеры множителя и кнопок).
- [ ] Добавить экран «Топ» и «Профиль», чтобы вкладки в нижней навигации были функциональными.
- [ ] Вынести типы (`Player`, `RoundState`, `Bet`) в отдельный модуль `src/types/crash.ts` и подготовить слой сервисов для будущего API/WebSocket.
- [ ] Добавить дополнительные тесты: сценарии проигрыша без кэш‑аута, edge‑кейсы по ставкам (0, очень большие числа).

## 🐛 ИСТОРИЯ ПРОБЛЕМ И ИСПРАВЛЕНИЙ
### 25.02.2026 — Проблемы с тестовой средой и ассетами
- Проблема: Vitest не запускался из‑за отсутствия `jsdom` в окружении.
  - Решение: установлена зависимость `jsdom` и настроено окружение `test.environment = "jsdom"` в `vite.config.ts`.
  - Учтите: для любых новых тестов, завязанных на DOM, jsdom уже доступен.
- Проблема: тесты падали из‑за импорта локального изображения зайца как модуля (`import ... from "../../../assets/..."`), которого нет в структуре проекта.
  - Решение: заменён модульный импорт на строковый путь `/bunny-rocket.jpg`, не требующий наличия файла на этапе сборки/тестов.
  - Учтите: для отображения картинки в браузере нужно вручную положить соответствующий файл в `public/bunny-rocket.jpg`.

## 📊 ТЕКУЩИЙ СТАТУС
- Проект успешно собирается и запускается как SPA на React + Vite.
- Экран Crash визуально повторяет макет: верхняя панель, центральный множитель, ряд последних коэффициентов, кнопка «Сделать ставку», список игроков, нижняя навигация.
- Логика роста множителя, краша и ставки работает на фронтенде; есть базовые автоматические тесты.
- Пока нет реального бэкенда, мультиплеер имитируется статическими/рандомными данными на клиенте.

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ
- Главная точка входа интерфейса — компонент `CrashScreen`; логика игры инкапсулирована в хуке `useCrashGame`.
- Движок краша построен таким образом, чтобы позже можно было заменить внутреннюю генерацию `maxMultiplier` и тик‑таймер на события от сервера/WebSocket.
- Все стили сейчас находятся в `global.css` и `app-layout.css`; при дальнейшем росте проекта стоит постепенно выносить стили в модули или использовать CSS‑in‑JS.
- Для интеграции с реальным бэкендом важно будет сохранить разделение: UI (компоненты) ↔ логика игры (`useCrashGame`) ↔ слой данных (будущие API‑сервисы).

